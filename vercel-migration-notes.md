# Estado de migración a Vercel

## 2026-08-11

- Existe un proyecto de Vercel llamado `elora-smart`, conectado al repositorio `raulplazavera211-bit/elora-smart` y desplegando automáticamente la rama `main` en `elora-smart.vercel.app`.
- El proyecto muestra una prueba Pro con vencimiento en 14 días y, en la última verificación, el panel todavía mostraba el aviso **Add a Card**.
- No hay base de datos ni almacenamiento conectados al proyecto todavía.
- Los activos actuales recuperados para una futura migración suman aproximadamente 1,7 GB, por lo que deben alojarse en almacenamiento externo y no dentro del paquete de despliegue.
- La interfaz y rutas existentes no deben modificarse durante la migración. La adaptación añadida solo reutiliza la API Express como función de Vercel y conserva rutas públicas.
- La prueba de Vercel se convirtió en un plan Pro tras añadirse un método de pago.
- El almacén público `elora-smart-blob` se creó correctamente en la región IAD1 y quedó conectado al proyecto con la variable `BLOB_READ_WRITE_TOKEN`, necesaria para la carga de activos.
- El recurso Blob aparece en el panel de almacenamiento como público y creado; la configuración de base de datos queda pendiente de selección y creación.
- El usuario autorizó expresamente la creación de Neon PostgreSQL; Neon fue seleccionado en el asistente de base de datos de Vercel y está pendiente de confirmación en el proveedor.
- Neon ofrece el plan Free seleccionado por defecto en la región IAD1, alineada con el almacén Blob. La autenticación integrada de Neon debe dejarse desactivada porque la aplicación conserva su propio flujo de administración.
- El asistente de Neon se cerró antes de la confirmación de configuración y fue reabierto; todavía no se ha creado ninguna base de datos.
- En el nuevo intento se volvió a seleccionar Neon y el plan Free; el asistente está en el paso final `Accept and Create`, cuya aceptación ya fue confirmada por el usuario.
- Al confirmar la instalación, Vercel indica `Integration already installed` y no avanza a la creación de la base de datos. Debe revisarse la integración ya instalada o completar la creación desde el panel de Neon.
- La sección de integraciones de Vercel confirma que Neon está instalada y ofrece acceso a su consola. La navegación del navegador se reinició antes de abrirla, por lo que se retomará desde una URL directa.
- La consola de Neon confirma que la organización `Vercel: Elora Smart` no tiene proyectos y exige crearlos mediante la integración de Vercel; la creación debe resolverse desde el asistente de Vercel.
- La sección de gestión de la integración muestra que el producto `Neon Serverless Postgres` aún requiere el paso `Install`; se usará la integración nativa de Neon, con facturación vía Vercel, para crear la base de datos administrada.
- La instalación nativa de Neon está configurada con el plan Free y región IAD1. Se desactivó su autenticación integrada para no modificar el flujo de acceso propio de Elora Smart.
- El asistente de Neon muestra el resumen de creación: región IAD1, autenticación desactivada y plan Free. Solo queda asignar un nombre operativo y crear la base de datos.
- La base de datos `elora-smart-production` se creó correctamente en Neon; queda finalizar la vinculación del recurso con el proyecto de Vercel y migrar el esquema y los datos.
- La vinculación de Neon apunta al proyecto `elora-smart` y selecciona los entornos Production y Preview. El prefijo de variable se ajustará a `DATABASE` para generar `DATABASE_URL`, que utiliza el backend actual.
- Se verificó que la opción `elora-smart` está disponible en el selector de proyecto de la integración; la conexión permanece deshabilitada hasta confirmar explícitamente dicha opción.
- Los entornos Production y Preview están seleccionados y el prefijo `DATABASE` es válido, pero el selector de proyecto todavía conserva el campo de búsqueda vacío; se repetirá la selección mediante interacción visible para que la integración habilite `Connect`.
- La integración Neon se conectó correctamente con `elora-smart`. En el proyecto de Vercel ya aparecen `DATABASE_URL`, `DATABASE_URL_UNPOOLED` y las variables auxiliares de conexión; la base `elora-smart-production` está disponible en el plan Free.
- Se instaló el controlador serverless de Neon y se añadió un esquema PostgreSQL paralelo con los mismos nombres de tablas y columnas. La migración generada crea cinco enums y once tablas nuevas, sin sentencias `DROP`, `ALTER` destructivas ni borrado de datos.
- El SQL de creación revisado quedó cargado en el editor de Neon con el modo de escritura habilitado; el siguiente paso es ejecutarlo y verificar que las once tablas se hayan creado correctamente antes de migrar datos.
- La primera ejecución no modificó el esquema: el editor había truncado parcialmente una sentencia y Neon devolvió un error de sintaxis. Se sustituyó el contenido mediante el mecanismo nativo del editor y se comprobará el estado actual con una consulta de solo lectura antes de reintentar la creación en lotes pequeños.
- La consulta de diagnóstico devolvió cero tablas en `public`, por lo que Neon no conserva objetos parciales tras el error. La creación se repetirá con reemplazo nativo del editor y lotes cortos, empezando por los enums y verificando cada resultado.
- El editor SQL de la integración usa sentencias preparadas y rechaza varios comandos por ejecución. Los cinco enums y las once tablas se crearán de forma individual, verificando el resultado de cada paso para preservar la atomicidad y evitar estados incompletos.
- Se creó y verificó correctamente el enum `coupon_type` en Neon mediante una única sentencia. Los tipos restantes y las tablas se continuarán aplicando con una sola unidad transaccional por lote.
- Los cuatro enums restantes (`order_status`, `payment_method_type`, `payment_status` y `user_role`) se crearon correctamente dentro de un bloque PostgreSQL `DO`. Con los cinco tipos disponibles, se puede crear el lote atómico de once tablas.
- El bloque transaccional de tablas se ejecutó correctamente en Neon. Se crearon las once tablas del esquema sin cambios destructivos; el siguiente control verificará el inventario de tablas y sus recuentos antes de transferir datos desde MySQL.
- La verificación del catálogo en Neon confirmó exactamente once tablas: `admin_credentials`, `club_elora_signups`, `contact_submissions`, `coupons`, `experience_slides`, `order_items`, `orders`, `payment_methods`, `products`, `site_popups` y `users`.
- Se inventariaron los datos de MySQL y se generaron lotes PostgreSQL temporales, sin añadir datos al repositorio. El editor de Neon está de nuevo en modo de escritura y se transferirán los lotes uno a uno, comprobando después que los recuentos coincidan.
- El lote de usuarios se transfirió correctamente y se actualizó la secuencia de identificadores. Los lotes restantes se combinarán en una única transacción de datos para conservar su integridad y posteriormente se comprobarán los recuentos de las once tablas.
- El catálogo de seis productos se transfirió correctamente al esquema de Neon. El editor no procesa de forma fiable un lote de 68 KB de una vez, por lo que los registros restantes se cargarán en lotes por tabla y se validarán mediante recuentos al finalizar.
- Los seis métodos de pago, incluyendo su orden y configuración, se transfirieron correctamente a Neon. Los siguientes registros de poco volumen se agruparán en un lote transaccional pequeño; los pedidos y sus líneas se aplicarán por separado.
- Los dos cupones existentes, incluido `SILVIO` con su importe y restricción de producto, se transfirieron correctamente y la secuencia quedó sincronizada.
- Las credenciales administrativas se transfirieron correctamente conservando el hash de contraseña y la secuencia de identificadores.
- Las tres suscripciones existentes del Club Elora se transfirieron correctamente y se sincronizó su secuencia de identificadores.
- Las dieciocho solicitudes de contacto existentes se transfirieron correctamente y la secuencia de identificadores quedó sincronizada. Quedan por transferir los pedidos y sus líneas antes de ejecutar la verificación final de recuentos.
- El primer lote de diecinueve pedidos históricos se transfirió correctamente a Neon. El segundo lote actualizará la secuencia de identificadores antes de migrar sus líneas de pedido.
- El segundo lote de diecinueve pedidos se transfirió correctamente y la secuencia de `orders` quedó sincronizada. A continuación se migrarán las cincuenta y siete líneas asociadas en tres lotes controlados.
- El primer lote de veinte líneas de pedido se transfirió correctamente a Neon. Se transferirán dos lotes adicionales antes de sincronizar la secuencia de `order_items` y comparar los recuentos globales.
- El segundo lote de veinte líneas de pedido se transfirió correctamente a Neon. El último lote contiene diecisiete líneas y sincronizará la secuencia de `order_items`.
- El último lote de diecisiete líneas se transfirió correctamente y la secuencia de `order_items` quedó sincronizada. Todos los datos con registros se han cargado en Neon; se verificará ahora que los recuentos coincidan exactamente con MySQL.
- La verificación final confirmó que todos los recuentos en Neon coinciden con el inventario de MySQL: 1 usuario, 1 credencial administrativa, 6 productos, 6 métodos de pago, 2 cupones, 38 pedidos, 57 líneas de pedido, 18 solicitudes de contacto, 3 altas del Club Elora y 0 registros en las dos tablas de configuración vacías. La migración de datos ha concluido sin discrepancias.
- La consola de Vercel confirma que `BLOB_READ_WRITE_TOKEN` y `BLOB_STORE_ID` están configurados como variables del proyecto en Production y Preview; el token permanece sensible y no está disponible en el entorno local de desarrollo. Los 128 activos preparados ocupan aproximadamente 1,7 GB y requieren una transferencia autenticada hacia el Blob Store.
- Se añadió `scripts/import-vercel-blob-assets.mjs` y se integró en `build:vercel`. El importador solo se activa cuando Vercel inyecta `BLOB_READ_WRITE_TOKEN`, obtiene los 128 activos desde una fuente temporal autorizada, mantiene sus claves originales y omite las claves ya presentes para que los despliegues posteriores sean reanudables. La comprobación de tipos, la suite Vitest y la compilación local han finalizado correctamente; el siguiente despliegue de Vercel activará la importación real.
- El despliegue de producción `50c569c` terminó correctamente en Vercel en 1 minuto y 33 segundos, quedó marcado como actual y se publicó en `https://elora-smart.vercel.app`. El siguiente control verificará directamente el inventario del Blob Store y las rutas `/manus-storage/*`; la fuente temporal seguirá activa hasta cerrar esa verificación.
- La interfaz de Vercel Blob confirma que la importación se ejecutó: aparecen los activos originales con sus claves preservadas (por ejemplo, `1_583ff2d7.jpeg`, `AURA-BLANCO-2-scaled_95299e91.jpg` y `AURA-suspendido-p-800x800_a7fc7d94.jpg`) y sus tipos y tamaños correctos. Todos figuran como cargados durante el despliegue de importación; queda por comprobar el recuento integral y la resolución a través del proxy `/manus-storage/*`.
- La primera prueba de `https://elora-smart.vercel.app/manus-storage/1_583ff2d7.jpeg` devolvió `500 FUNCTION_INVOCATION_FAILED` (solicitud `iad1::n4dnl-1786479377500-d6a475684888`). La importación de Blob está confirmada, pero la función de proxy requiere diagnóstico antes de que los activos puedan considerarse validados.
- El registro de producción identificó la causa: la función compilada intentaba importar `/var/task/server/app`, que no estaba presente en el paquete ESM. Se sustituyó el adaptador TypeScript por `api/[...path].mjs`, que carga `dist/serverless/app.mjs`; este bundle se genera desde `server/vercelHandler.ts` con esbuild y se incluye explícitamente en la función. Tipos, Vitest y compilación local con el paquete serverless finalizaron correctamente; queda la validación en el siguiente despliegue de Vercel.
- La segunda ejecución confirmó que el módulo ausente quedó resuelto, pero la aplicación completa carga servicios de pago y autenticación al inicializar. En Vercel aún faltan `OAUTH_SERVER_URL` y `RESEND_API_KEY`, y `Resend` aborta la función antes de que alcance el proxy. Se aislará `/manus-storage/*` en una función mínima que solo registre el proxy Blob, sin afectar a las rutas API generales ni inicializar esos servicios.
- Se añadió `api/storage.mjs` con un bundle dedicado `dist/serverless/storage.mjs` creado desde `server/vercelStorageHandler.ts`. La ruta `/manus-storage/*` se dirige a esa función mínima, mientras que `/api/*` mantiene el adaptador Express completo. La comprobación de tipos, Vitest y la compilación local verificaron ambos bundles (`app.mjs` y `storage.mjs`) sin errores.
- El despliegue `862fffff` finalizó correctamente y la validación de producción de `https://elora-smart.vercel.app/manus-storage/1_583ff2d7.jpeg` respondió con una redirección al objeto público de Vercel Blob. El navegador cargó correctamente la imagen original de 800×800; la transferencia de activos y el proxy de compatibilidad quedan verificados.
- El panel de Blob confirma 1,75 GB almacenados y muestra los objetos importados con fechas coherentes. La consulta directa de su API interna desde el navegador requiere un token de sesión específico y devuelve 403 sin él, por lo que la comprobación integral se completará mediante la paginación visible y rutas representativas, sin extraer ni exponer credenciales sensibles.
- La carga visible del inventario ya confirma los tipos de activos previstos —imágenes JPEG, PNG y WebP, además de las fichas técnicas PDF— con una misma marca temporal de importación. El listado continúa en más lotes, por lo que se contrastará su número final con el manifiesto local de 128 archivos antes de cerrar esta tarea.
- Las rutas representativas del proxy se validaron para cuatro tipos: la imagen JPEG se visualizó desde Blob, el vídeo MP4 se redirigió al objeto público, el PDF de AURA se abrió correctamente en el visor y la fuente WOFF2 se descargó desde su URL histórica. El panel muestra 1,75 GB en Blob. Las APIs internas de conteo requieren una autorización no accesible mediante las cookies de la página, por lo que no se expondrán secretos para obtenerla.
- La fuente local contenía exactamente 128 archivos y 1.746.868.429 bytes (1,7 GB), coherentes con los 1,75 GB mostrados por Vercel Blob. Tras la importación verificada y las pruebas de tipos, el importador se condicionó a `BLOB_IMPORT_ASSETS=1`, que no está definido; por tanto, futuros despliegues no requieren la fuente temporal. El servidor temporal se cerró y la compilación completa siguió pasando sin acceder a ella.
- La búsqueda explícita de `BLOB_IMPORT_ASSETS` en las Variables de Entorno de Vercel no devolvió resultados, cubriendo todos los entornos visibles. El interruptor de importación permanece desactivado; se publicará una última versión autónoma para comprobarlo en Vercel antes de eliminar cualquier dependencia temporal pendiente.
- El navegador no puede leer el servidor temporal local de forma fiable, por lo que los lotes restantes se inyectarán directamente desde los archivos temporales por tabla. Esta limitación no altera los datos originales ni el esquema de Neon.
