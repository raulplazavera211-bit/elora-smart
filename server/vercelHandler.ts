import { createApp } from "./app";

/**
 * Entrada mínima para el paquete ESM que Vercel ejecuta como función serverless.
 * El script `build:vercel` agrupa este módulo con sus dependencias internas.
 */
export default createApp();
